<UserControl x:Class="Callen.Pages.ListSearch"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="575" d:DesignWidth="800">
    <Grid Margin="0">
        <DataGrid Name="grdColec" Style="{DynamicResource dataGrid}"
                  ColumnHeaderStyle="{DynamicResource DGColumnHeader}" CellStyle="{DynamicResource DGCell}"
                  BorderThickness="0"
                  AutoGenerateColumns="False"
                  VerticalScrollBarVisibility="Visible"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling">
            <DataGrid.ItemContainerStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="MinHeight" Value="30" />
                    <Setter Property="SnapsToDevicePixels" Value="True" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="Foreground" Value="{DynamicResource darkHighLight}" />
                    <Setter Property="VerticalAlignment" Value="Center" />
                    <EventSetter Event="MouseDoubleClick" Handler="Row_DoubleClick" />
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FFC2C7CB" />
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource darkHighLight}" />
                            <Setter Property="Foreground" Value="#FFF0F2F3" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.ItemContainerStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path=id}" IsReadOnly="True" Header="{DynamicResource Grid.Col.Id}"
                                    ElementStyle="{DynamicResource DGElemntCenter}" 
                                    Width="0.5*" />
                <DataGridTextColumn Binding="{Binding Path=name}" IsReadOnly="True" Header="{DynamicResource Grid.Col.Name}" 
                                    ElementStyle="{DynamicResource DGElementLeft}"
                                    Width="3*" />
                <DataGridTextColumn Binding="{Binding Path=year}" IsReadOnly="True" Header="{DynamicResource Grid.Col.Year}" 
                                    ElementStyle="{DynamicResource DGElemntCenter}" 
                                    Width="0.8*" />
                <DataGridTextColumn Binding="{Binding Path=matrix}" IsReadOnly="True" Header="{DynamicResource Grid.Col.Matrix}" 
                                    ElementStyle="{DynamicResource DGElementLeft}"
                                    Width="1.0*" />
                <DataGridTextColumn Binding="{Binding Path=collection}" IsReadOnly="True" Header="{DynamicResource Grid.Col.Collection}" 
                                    ElementStyle="{DynamicResource DGElementLeft}"
                                    Width="1.0*" />
                <DataGridTextColumn Binding="{Binding Path=code}" IsReadOnly="True" Header="{DynamicResource Grid.Col.Folder}"
                                    ElementStyle="{DynamicResource DGElemntCenter}" 
                                    Width="0.65*"
                                    MinWidth="84" />
                <DataGridTextColumn Binding="{Binding Path=theme}" IsReadOnly="True" Header="{DynamicResource Grid.Col.Theme}" 
                                    ElementStyle="{DynamicResource DGElementLeft}"
                                    Width="1.3*" />
                <DataGridTextColumn Binding="{Binding Path=description}" IsReadOnly="True" Header="{DynamicResource Grid.Col.Description}" 
                                    ElementStyle="{DynamicResource DGElementLeft}"
                                    Width="2*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
