<Window x:Class="Callen.Windows.Other.ImageZoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="clr-namespace:Callen"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="ImageZoomWindow"
        AllowsTransparency="True"
        WindowStyle="None"
        Background="{x:Null}"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Grid>
        <!-- Full-window overlay -->
        <Border x:Name="closeBorder"
                Style="{StaticResource DialogOverlayDimmer}"
                MouseLeftButtonDown="Border_MouseLeftButtonDown" />

        <!-- Content layer -->
        <Grid>
            <Grid HorizontalAlignment="Center"
                  VerticalAlignment="Center">
                <!-- Centered image -->
                <Image x:Name="img"
                       Stretch="Uniform"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"
                       RenderOptions.BitmapScalingMode="HighQuality"
                       Visibility="Hidden"
                       MaxHeight="0"/>

                <!-- Close button pinned to image corner -->
                <Button x:Name="btn_close"
                        Click="btn_close_Click"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Margin="2"
                        Width="27"
                        Height="27">
                    <Button.Style>
                        <Style TargetType="{x:Type Button}" BasedOn="{StaticResource menuMinButton}">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="{x:Type Button}">
                                        <Border Name="border"
                                                Background="Transparent"
                                                BorderBrush="Transparent"
                                                BorderThickness="1"
                                                CornerRadius="4">
                                            <ui:TintedIcon x:Name="img"
                                                           MaskSource="{StaticResource Icon.CloseWindow}"
                                                           Tint="#FF2E3136"
                                                           Width="{StaticResource dialogCloseIconSize}"
                                                           Height="{StaticResource dialogCloseIconSize}"
                                                           RenderOptions.BitmapScalingMode="HighQuality" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#33000000" />
                                                <Setter Property="Cursor" Value="Hand" />
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="border" Property="Background" Value="#59000000" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Grid>
    </Grid>
</Window>






