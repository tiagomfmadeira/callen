<UserControl x:Class="Callen.Pages.proSearch"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Callen.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="575" d:DesignWidth="800">
    <Grid Style="{StaticResource gridStyle}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="60"/>
            </Grid.RowDefinitions>
        
            <Border BorderBrush="{DynamicResource darkestHighLight}" Style="{StaticResource simpleBorder}">
                <DataGrid Name="grdColec" Style="{DynamicResource dataGrid}" ColumnHeaderStyle="{DynamicResource DGColumnHeader}" CellStyle="{DynamicResource DGCell}" AutoGenerateColumns="False">
                    <DataGrid.ItemContainerStyle>
                        <Style TargetType="DataGridRow">
                            <EventSetter Event="MouseDoubleClick" Handler="Row_DoubleClick"/>
                        </Style>
                    </DataGrid.ItemContainerStyle>
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Binding="{Binding Path=favourite}" IsReadOnly="False" Header="Fav" CellStyle="{DynamicResource DGCell}">
                            <DataGridCheckBoxColumn.ElementStyle>
                                <Style TargetType="{x:Type CheckBox}">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type CheckBox}">
                                                <Border Background="Transparent" >
                                                    <Image Name="img" Source="../Icons/Fat/favorite.png" RenderOptions.BitmapScalingMode="HighQuality" Width="17"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="img" Property="Source" Value="../Icons/Fat/favoriteChecked.png"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </DataGridCheckBoxColumn.ElementStyle>
                        </DataGridCheckBoxColumn>

                        <DataGridTextColumn Binding="{Binding Path=ID}" IsReadOnly="True" Header="ID"/>
                        <DataGridTextColumn Binding="{Binding Path=name}" IsReadOnly="True" Header="Nome" MinWidth="50"/>
                        <DataGridTextColumn Binding="{Binding Path=descr}" IsReadOnly="True" Header="Descrição"  MaxWidth="500"/>
                        <DataGridTextColumn Binding="{Binding Path=year}" IsReadOnly="True" Header="Ano" MinWidth="50"/>
                        <DataGridTextColumn Binding="{Binding Path=folder}" IsReadOnly="True" Header="Pasta" MinWidth="50"/>
                        <DataGridTextColumn Binding="{Binding Path=theme}" IsReadOnly="True" Header="Tema" MinWidth="50"/>
                        <DataGridTextColumn Binding="{Binding Path=peer}" IsReadOnly="True" Header="Patrocinador" MinWidth="100"/>
                        <DataGridTextColumn Binding="{Binding Path=sponsor}" IsReadOnly="True" Header="Fornecedor" MinWidth="50"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <Button Click="formPage" Grid.Row="1" Style="{StaticResource addButton}"/>
        </Grid>
    </Grid>
</UserControl>
